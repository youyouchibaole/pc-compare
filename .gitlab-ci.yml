pages:
  stage: deploy
  tags:
    - ci-fed-publish
  script:
    - mkdir -p public
    - cp index.html public/
    - cp -r Assets public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
